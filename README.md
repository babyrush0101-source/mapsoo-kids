
  # baseul kids

  This is a code bundle for baseul kids. The original project is available at https://www.figma.com/design/fAex8hoSxOuGWBhljJJIUg/baseul-kids.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  