# Mapsoo Worldsmith's portable v0.1 alpha requires no environment variables.
#
# Never commit API keys or service-role credentials. A future hosted provider
# must document whether its key stays in a local session or is protected by a
# server-side proxy before adding a named variable here.
